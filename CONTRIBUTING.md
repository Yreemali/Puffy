# Contributing to puffy

Keep real-time audio code allocation-free and non-blocking in callbacks. Do not add key logging, typed-text capture, telemetry, or proprietary virtual-audio dependencies. Build and run `ctest` before submitting changes. Platform-specific code belongs under its platform adapter and must document permissions, installation, and recovery behavior.
